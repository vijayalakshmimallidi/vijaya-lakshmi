#include <stdio.h>
int main()
{
	int a,b,sum,diff;
	printf("enter a and b values");
	scanf("%d%d",&a,&b);
	sum=a+b;
	diff=a-b;
	printf("\n sum of the digits=%d",sum);
		printf("\n diff of digits=%d",diff);
	return 0;
}
